/**
 * Hydration (not applicable to CLI)
 */

/**
 * Hydrate template (not needed)
 * Replaces: $.hydrate_template()
 */
export function hydrate_template(): null {
    return null;
}

/**
 * Next hydration node (not needed)
 * Replaces: $.next()
 */
export function next(): null {
    return null;
}

/**
 * Reset hydration (not needed)
 * Replaces: $.reset()
 */
export function reset(): void {
    // No-op
}
